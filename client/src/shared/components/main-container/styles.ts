import { css } from "@styled-system/css";

export const authLayout = css({
    display: "flex",
    flexDirection: "column",
    minHeight: "100vh",
    bg: "surfacePageColor",
    color: "textColor",
});
