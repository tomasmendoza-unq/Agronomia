import { css } from "@styled-system/css";

export const panel = css({
    width: "100%",
    minWidth: 0,
    height: "100%",
    bg: "surfacePageColor",
});

export const h1 = css({
    fontSize: "32px",
    fontWeight: "bold",
    marginBottom: "4",
});
