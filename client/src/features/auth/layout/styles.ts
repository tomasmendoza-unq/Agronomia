import { css } from "@styled-system/css";

export const authLayout = css({
    display: "flex",
    flexDirection: "column",
    paddingLeft: "4",
    minHeight: "100vh",
    bg: "surfacePageColor",
    color: "textColor",
});
